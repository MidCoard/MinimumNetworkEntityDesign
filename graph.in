# we decide to use entities from 1
# we decide to use ports from 0
7 6
# you can specify the mac and ip address of the entity
# (make sure it belongs to its network segment and you should provide its gateway(or default ))
# and the socket ip of the physical layer
# i.e. PC mac ip(gateway) physical_layer_ip
PC 10.0.0.2(10.0.0.1)
PC 10.0.0.3(10.0.0.1)
PC
PC
# you can specify the network segment of the entity or it will be automatically generated by its subnet ip
# i.e. router network_segment subnet_mask
ROUTER
SWITCHER
SWITCHER
# you can determine the port of the entity or not (automatically generated)
1 (6,1) # use port 1 of entity 6
2 6 # not specified, use next port of the entity 6
3 (6,2) # use port 2 of entity 6
4 7
6 5
7 5